version: '3'
services:
  cms:
    networks:
      - facile
    build: .
    restart: always
    ports:
      - '24040:24040'
    volumes:
      - .:/home/node/cms
networks:
  facile:
    external:
      name: facile
