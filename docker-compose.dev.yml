version: '3'
services:
  cms:
    networks:
      - facile
    build: .
    ports:
      - '8000:8000'
      - '9000:9000'
    volumes:
      - .:/home/node/app
    entrypoint: 'npm run dev'
networks:
  facile:
    external:
      name: facile
